# scaling-foundations / lab1 환경 변수
# cp .env.example .env 로 복사한 뒤 값을 조정한다. .env 는 커밋되지 않는다.

POSTGRES_USER=scaling
POSTGRES_PASSWORD=scaling
POSTGRES_DB=scaling

# 현재 실행 중인 stage. Makefile 의 stage<n> 타겟이 자동으로 설정한다.
STAGE=1
